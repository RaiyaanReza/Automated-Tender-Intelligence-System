"""
gemini_util.py

Placeholder for Gemini 1.5 Flash API integration utilities.

Expected responsibilities:
- Wrap calls to the Gemini API (or other LLM providers)
- Provide helpers for summarization, extraction, and scoring
"""

from typing import Any, Dict


def summarize_text(text: str, options: Dict[str, Any] = None) -> str:
    """Return a summarized version of `text` using configured LLM.

    Placeholder implementation.
    """
    raise NotImplementedError("gemini_util.summarize_text must be implemented by backend team")
